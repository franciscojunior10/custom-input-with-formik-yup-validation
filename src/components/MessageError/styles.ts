import styled from 'styled-components';

export const Content = styled.span`
  display: flex;
  align-items: center;
  margin-top: 8px;
  color: var(--color-error);
  font-size: 12px;

  img {
    margin-right: 2px;
  }
`;
